package com.project.sethu.inventory_store.config;

import org.springframework.context.annotation.Configuration;

// CORS is now configured in SecurityConfig.java via corsConfigurationSource()
// This file is intentionally left empty to avoid duplicate CORS filter conflicts.
@Configuration
public class CorsConfig {
}
